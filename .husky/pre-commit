echo "🔍 Running pre-commit checks..."

# Run staged-file formatting and linting through Vite+
echo "📝 Checking staged files..."
vp staged

# Run full format, lint, and TypeScript checks through Vite+
echo "🔍 Running quality checks..."
vp check
vp exec tsc --noEmit

echo "✅ Pre-commit checks completed!"
